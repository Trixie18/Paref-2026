{
  "deploy": {
    "startCommand": "if [ -n \"$GOOGLE_SERVICE_ACCOUNT_JSON\" ]; then printf '%s' \"$GOOGLE_SERVICE_ACCOUNT_JSON\" > /app/service-account.json; fi; exec uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
